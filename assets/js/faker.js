functions buildDeck  (have an Object called Deck)
		  shuffleDeck (deck)
		  dealCard (deck)
		  hitCard (deck)		  
		  testHand (variables for hand)
		  testGame (hand1, hand2)

to build a buildDeck [numbers, suits, images]
*End the Game(do you want to play again?)*

First Build the Deck